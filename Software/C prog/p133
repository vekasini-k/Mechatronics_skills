#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int c=1;
    for(int j=0;j<n;j++){
        for(int k=0;k<n;k++){
            if(arr[j]==arr[k]&&j!=k){
                c=0;
            }
        }
        if(c==1){
            printf("%d",arr[j]);
        }
        c=1;
    }

    return 0;
}
