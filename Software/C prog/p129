#include <stdio.h>
int main(){
    int n,k;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&k);
    for(int j=k-1;j<n;j++){
        arr[j]=arr[j+1];
    }
    for(int a=0;a<n-1;a++){
        printf(" %d",arr[a]);
    }

    return 0;
}
