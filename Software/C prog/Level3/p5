#include <stdio.h>
int main(){
    int n,t=0;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
    scanf("%d",&arr[i]);
    }
    int k;
    scanf("%d",&k);
    for(int i=0;i<k;i++){
        t=arr[n-1];
        for(int j=n-1;j>0;j--){
            arr[j]=arr[j-1];
        }
        arr[0]=t;

    }
    for(int i=0;i<n;i++){
    printf("%d\n",arr[i]);
    }
    
    return 0;
}
