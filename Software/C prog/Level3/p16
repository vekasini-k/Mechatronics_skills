#include <stdio.h>
int main(){
    int n,isPrime;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n;i++){
        if(arr[i]<=1){
            printf("%d",arr[i]);
        }
        else{
        isPrime=1;
        for(int j=2;j<=arr[i]/2;j++){
            if(arr[i]%j==0){
            isPrime=0;
            break;
            }    
        }
        if(isPrime==0){
            printf(" %d",arr[i]);
        }
    }
    }
    return 0;
}
