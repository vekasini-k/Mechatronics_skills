#include <stdio.h>
int main(){
    int n,k;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&k);
    for(int i=0;i<n;i++){
        if(arr[i]==k){
            for(int j=0;j<n;j++){
                arr[j]=arr[j+1];
            }
            break;
        }
    }
    for(int j=0;j<n-1;j++){
        printf("%d",arr[j]);
    }
    return 0;
}
