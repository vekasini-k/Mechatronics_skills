#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int s=arr[0]+arr[1]+arr[n-1]+arr[n-2];
    printf("%d",s);

    return 0;
}
