#include <stdio.h>
int main(){
    int n,k,c;
    scanf("%d",&n);
    int arr[n];
    for (int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&k);
    c=0;
    for(int j=0;j<n;j++){
        if(k==arr[j]){
            c++;
        }
    }
        printf("%d",c);

    return 0;
}
