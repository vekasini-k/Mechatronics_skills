#include <stdio.h>
int main(){
    char n[100];
    int c=0;
    scanf("%s",&n);
    for (int i=0;n[i]!='\0';i++){
        char ch=n[i];
        if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u'){
            c++;
        }
    }
    printf("%d",c);

    return 0;
}
