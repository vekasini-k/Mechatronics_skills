#include <stdio.h>
int main(){
    int n,sum=0;
    scanf("%d",&n);
    while (n>=10){
        int sum=0;
        while (n>0){
         int a=n%10;
         sum=sum+a;
         n=n/10;
        } 
        n=sum;
    }
    printf("%d",n);
    return 0;
}
