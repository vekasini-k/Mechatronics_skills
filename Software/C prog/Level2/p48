#include <stdio.h>
int main(){
    int n,sum=1;
    scanf("%d",&n);
    for(int i=n;n!=0;i++){
        int a=n%10;
        sum=sum*a;
        n=n/10;
    }
    if(sum>0){
    printf("%d",sum);
    }
    else if(sum==0){
        printf("invalid");
    }
    return 0;
}
