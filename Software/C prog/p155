#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int k;
    scanf("%d",&k);
    for(int i=0;i<n;i++){
        if(arr[i]==k){
            for(int j=i;j<n-1;j++){
                arr[j]=arr[j+1];
            }
            n--;
        }
    }
    for(int i=0;i<n;i++){
        printf("%d",arr[i]);
    }


    return 0;
}
