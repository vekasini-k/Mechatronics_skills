#include <stdio.h>
int main(){
    int n,avg=0;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int s=0;
    for(int j=0;j<n;j++){
        s=s+arr[j];
    }
    avg=s/n;
    printf("%d",avg);

    return 0;
}
