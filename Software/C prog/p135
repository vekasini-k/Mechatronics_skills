#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int t;
    for(int j=0;j<n;j++){
        for(int k=j+1;k<n;k++){
            if(arr[j]>arr[k]){
                t=arr[j];
                arr[j]=arr[k];
                arr[k]=t;
            }
        }
    }
    for(int a=0;a<n;a++){
        printf(" %d",arr[a]);
    }

    return 0;
}
